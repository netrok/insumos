# Insumos

Proyecto **Insumos** (Laravel + Vite) para gestión interna.

## Requisitos
- PHP (vía **Laravel Herd**)
- Composer
- Node.js + npm
- PostgreSQL

## Setup local (Herd + Postgres + Vite)

### 1) Clonar e instalar dependencias
```bash
git clone <REPO_URL>
cd insumos
composer install
npm install
